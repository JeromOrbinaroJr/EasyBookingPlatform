<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header class="header">
        <nav class="navbar">
            <a href="/" class="nav-link">Главная</a>
        </nav>
    </header>

    <main class="main-content">
        <h1>Расписание и Столики</h1>
        <p>Выберите столик для бронирования:</p>
        
        <div class="table-field">
            <% tables.forEach((table, index) => { %>
                <div class="table-point <%= table.booked ? 'booked' : 'free' %>"
                    data-id="<%= index %>" onclick="bookTable(<%= index %>)">
                    <%= index + 1 %>
                </div>
            <% }) %>
        </div>
        
        <script>
            function bookTable(index) {
                // Пример отправки данных на сервер для бронирования
                fetch(`/business/book-table/${index}`, { method: 'POST' })
                    .then(response => response.json())
                    .then(data => {
                        if (data.success) {
                            alert('Столик забронирован!');
                            window.location.reload(); // Перезагружаем страницу для обновления состояния
                        } else {
                            alert('Ошибка бронирования.');
                        }
                    });
            }
        </script>
    </main>

    <footer>
        <p>&copy; 2024 EasyBookingPlatform.</p>
    </footer>
</body>
</html>
